import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="header">
      <div className="header-content">
        <h1>Good morning, Bergen</h1>
        <button>Settings</button>
      </div>
    </header>
  );
};

export default Header;